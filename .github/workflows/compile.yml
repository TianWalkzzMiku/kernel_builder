name: Building Kernel

on: 
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest

    container: 
      image: zycromerz/kerneldocker:v3

    steps:
    - uses: actions/checkout@v2
    - name: Building Kernel
      env:
        TELEGRAM_TOKEN: ${{ 5207751984:AAEcTALJDEx0BlYgesFHgsYTGqLsL2jFZQY }}
        CHANNEL_ID: ${{ -1001781224906 }}
        K_BRANCH: ${{ secrets.K_BRANCH }}
        TOKED: ${{ secrets.TOKED }}
        RELEASE: ${{ secrets.RELEASE }}
        TC: ${{ secrets.TC }}
        COMPILE: ${{ secrets.COMPILE }}
      run: |
        wget https://raw.githubusercontent.com/TianWalkzzMiku/SR-Kernel/caf-4.4/.circleci/build.sh
        bash build.sh
